# Nhật ký buổi học — 2026-07-07 (C)

**Chủ đề:** Java-Backend — runtime [[Polymorphism]] / dynamic dispatch.

## Reviews
[REVIEW] Interface vs Abstract Class — Easy — 2026-07-07
- Hỏi: vì sao `implements` nhiều interface được mà `extends` chỉ một class? Trả lời fluent, không cần hint: "interface chỉ là lời hứa (không state/code) → không có gì đụng nhau; extends kéo theo toàn bộ state+code của cha → nhiều cha là xung đột." Cách nói sai cũ ("nhiều method") **không tái xuất** → trục đã dính. Đẩy lịch tới 2026-07-11 (interval 4, ease 270).

## Khái niệm hôm nay
### [[Polymorphism]] — Dynamic dispatch — `Understood` (Good)
Dạy challenge-first từ chính code hôm qua (`Context` giữ `INotify` nhưng chạy `send()` của Email).

- **Điểm mấu chốt bạn tự sửa:** ban đầu nói "gắn với Email từ lúc compile". Qua ví dụ `userInput` (compiler không thể biết trước Email hay Sms), bạn tự đảo sang **runtime** — đúng bản chất dynamic dispatch.
- **Nửa còn lại (vai trò compiler):** bạn nêu đúng — compiler soi **static type** `INotify` để gác method nào *gọi được*; `sendBulk()` không có trong interface nên bị chặn dù vật thể Email có thật.
- **Cặp thuật ngữ chốt:** static type (compiler, compile-time) = *gọi được gì*; dynamic type (JVM, runtime) = *chạy bản nào*.
- **Upcast vs downcast:** `INotify n = new Email()` = upcast (tự động, an toàn); `(Email) n` = downcast (viết tay, rủi ro nổ lúc runtime). Downcast nhiều = abstraction rò rỉ.

## Thực hành
`01 Practice/Polymorphism.java` — 3 thí nghiệm, tự điền TODO và chạy:
1. Vòng lặp `List<INotify>` lẫn Email+Sms, một dòng `n.send(...)` in ra khi EMAIL khi SMS → *thấy* dynamic dispatch.
2. `n.sendBulk()` không cast → compiler chặn (`The method ... is undefined for the type ...`). Chạm ranh giới static type.
3. `((EmailNotification) n).sendBulk(...)` chạy được → hiểu cast đổi static type compiler thấy *cho đúng biểu thức đó*.
- Lỗi gặp phải: gọi `.send()` lên cả `List` thay vì từng phần tử (`... undefined for the type List<INotify>`) → sửa bằng for-each. (Lỗi cú pháp Java, không phải concept.)

## Output
`02 Output/Static-vs-Dynamic-Type.md` — mẩu "câu trả lời phỏng vấn" tự viết: static vs dynamic type + dynamic dispatch xảy ra lúc nào.

## Feynman Check
> **Hỏi:** Vì sao Java không thể chốt bản `send()` nào chạy lúc compile, mà phải đợi runtime?
> **Bạn (lần 1, result-only):** "send là method trong interface được nhiều class dùng chung nên nó là dynamic method, phải đến runtime mới biết." → mới là mô tả/dán nhãn, chưa ra nhân.
> **Bạn (lần 2, sau probe):** "vì đến lúc runtime thì sự phụ thuộc mới được truyền vào, và method trong class được truyền vào mới kích hoạt." → **trúng cơ chế**: vật thể chỉ bị gán lúc runtime nên compile-time không có gì để chốt.
> **Đánh giá:** Good — đúng cơ chế nhưng cần 2 lần probe mới bóc tới nguyên nhân.

## Khái niệm thứ hai — [[Exception Handling]] (checked vs unchecked) — `Understood` (Good)
Dạy tiếp trong cùng buổi theo yêu cầu "học tiếp".
- Bạn tự tách trục: `IOException` = bất định **ngoài tầm kiểm soát** (bên thứ ba / mạng), chắc chắn có ngày xảy ra dù code đúng → **checked**, compiler **ép** `catch`/`throws`; `NullPointerException` = **bug trong logic mình viết**, sửa code là hết → **unchecked**, không ép.
- Nối được chữ "ép" tới stakes ngân hàng: ép xử lý = không cho **nuốt lặng lẽ** một thất bại tất yếu → tránh "trừ bên mình mà chưa cộng bên kia" (trạng thái bất nhất).
- Thực hành `01 Practice/ExceptionHandling.java`: chạm compile error thật (**`Unhandled exception type BankTransferException`** = cái "ép"); Thí nghiệm 3 xác nhận unchecked không bị ép (compile ngon, nổ lúc runtime).
- Slip đã sửa: ban đầu viết rollback bằng `debit` (trừ thêm) thay vì `credit` (hoàn lại); tự nhận ra "phải cộng lại" — rollback = **đảo ngược đúng thao tác đã làm**.
- Insight nối: "trừ rồi phải cộng, không thì hoàn" = hạt giống [[transaction]] / atomic → `@Transactional` ([[Spring]]) sau này.
> **Feynman:** "Java bắt viết catch/throws để ngăn tiền trừ trong hệ thống mình mà chưa cộng hệ thống đối diện." → trúng cơ chế + stakes → Good.

## Buổi sau
Bắt đầu [[Collections Framework]] — chọn đúng cấu trúc (List / Set / Map) + ý thức [[Big-O]] (chạm blind spot [[DSA]]). Mở bằng tình huống tra cứu "ID đã tồn tại chưa" trên hàng triệu bản ghi.
