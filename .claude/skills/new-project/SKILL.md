---
description: Create a new sub-project under a brain's 03 Projects/ folder, scaffolding its CLAUDE.md, COMMANDS.md, and learning folders. Use for /new-project [brain] [project-name].
argument-hint: "[brain] [project-name]"
disable-model-invocation: true
---

The user invoked `/new-project`. Follow the playbook below EXACTLY — it is the complete operating procedure for scaffolding a sub-project. Confirm the brain, project name, and structure with the user before creating folders.

Raw arguments passed to the command: $ARGUMENTS

Open the playbook with the Read tool and follow it:
- Primary path: `C:/Users/minhdd_resolve/Desktop/WorkSpace/Learning-Factory/05 Skills/new-project.md`
- Fallback (ONLY if the primary path does not exist — e.g. the vault was moved or cloned to another machine): open `05 Skills/new-project.md` resolved from the git repository root, which is one level above the brain folders like `GCP/`.
