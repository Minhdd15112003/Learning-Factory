---
description: Close a learning session — run the Feynman interview, update the spaced-repetition schedule in every affected note, log Reasoning-Gaps, and write the session log. Use for /day-update, or when the user says "kết thúc buổi học" / "tổng kết hôm nay".
argument-hint: "[brain] [sub-project]"
---

The user invoked `/day-update`. Follow the playbook below EXACTLY — it is the complete operating procedure, including Step 1 (scope resolution). All spaced-repetition writes and new theory notes must land in the resolved scope's own `01 Ly thuyet/`, never a sibling/parent/child scope. Do NOT narrate what you read; go straight into the Feynman interview.

Raw arguments passed to the command: $ARGUMENTS

Open the playbook with the Read tool and follow it:
- Primary path: `C:/Users/minhdd_resolve/Desktop/WorkSpace/Learning-Factory/05 Skills/day-update.md`
- Fallback (ONLY if the primary path does not exist — e.g. the vault was moved or cloned to another machine): open `05 Skills/day-update.md` resolved from the git repository root, which is one level above the brain folders like `GCP/`.
