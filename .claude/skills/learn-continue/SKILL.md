---
description: Resume a learning session in this vault — load brain/sub-project context, run due spaced-repetition reviews, and open with a Socratic challenge. Use for /learn-continue [brain] [sub-project], or when the user asks to resume / continue / pick up where they left off.
argument-hint: "[brain] [sub-project]"
---

The user invoked `/learn-continue`. Follow the playbook below EXACTLY — it is the complete operating procedure, including Step 1 (launch + scope resolution from the current working directory and the arguments). Do NOT narrate what you read (Learning Mode Rule 8: no passive reporting); go straight into the session flow and end on a Socratic challenge.

Raw arguments passed to the command: $ARGUMENTS

Open the playbook with the Read tool and follow it:
- Primary path: `C:/Users/minhdd_resolve/Desktop/WorkSpace/gcp-document/05 Skills/learn-continue.md`
- Fallback (ONLY if the primary path does not exist — e.g. the vault was moved or cloned to another machine): open `05 Skills/learn-continue.md` resolved from the git repository root, which is one level above the brain folders like `GCP/`.
